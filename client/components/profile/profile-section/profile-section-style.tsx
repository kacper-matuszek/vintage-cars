import { createStyles, makeStyles } from "@material-ui/core/styles";

export const profileSectionStyle = makeStyles((theme) => 
createStyles({
    form: {
        width: '100%', //Fix IE 11 issue
        position: 'relative',
    }
}))